{
    "version": 2,
    "installCommand": "echo 'Dependencies bundled - skipping install'",
    "buildCommand": "echo 'Pre-built - skipping build'",
    "functions": {
        "dist/server.js": {
            "maxDuration": 30
        }
    },
    "routes": [{ "src": "/(.*)", "dest": "dist/server.js" }]
}
