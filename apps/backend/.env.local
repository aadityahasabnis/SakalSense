# Backend Environment Variables (Local Development)
# Copy from .env.example and fill with real values

PORT=8000
NODE_ENV=development

# Database (example PostgreSQL)
DATABASE_URL=postgres://user:password@localhost:5432/sakalsense_dev

# JWT/Auth
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Redis (if using)
REDIS_URL=redis://localhost:6379

# External APIs (dummy examples)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# AWS (for future deployment)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
