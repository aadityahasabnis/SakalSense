// Admin UI paths - Navigation routes for ADMIN stakeholder
// Usage: import { ADMIN_PATHS } from '@sakalsense/core';

export const ADMIN_PATHS = {
    login: '/admin/login',
    register: '/admin/register',
    dashboard: '/admin/dashboard',
    settings: '/admin/settings',
} as const;
