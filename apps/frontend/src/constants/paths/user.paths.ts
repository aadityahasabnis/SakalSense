// User UI paths - Navigation routes for USER stakeholder
// Usage: import { USER_PATHS } from '@sakalsense/core';

export const USER_PATHS = {
    login: '/login',
    register: '/register',
    dashboard: '/dashboard',
    profile: '/profile',
    settings: '/settings',
} as const;
